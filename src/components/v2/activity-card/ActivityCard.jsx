import { ArrowUpRight, MapPin } from 'lucide-react';
import './ActivityCard.scss';
export default function ActivityCard({ activity, onOpen }) { const date = activity.startTime ? new Date(activity.startTime) : null; return <article className="v2-activity-card"><div className="v2-activity-date"><strong>{date ? String(date.getDate()).padStart(2, '0') : '--'}</strong><span>THÁNG {date ? String(date.getMonth() + 1).padStart(2, '0') : '--'}</span></div><div className="v2-activity-copy"><div><small>{activity.clubName}</small><b>{activity.status === 'LIVE' ? 'Đang diễn ra' : 'Sắp diễn ra'}</b></div><h3>{activity.title}</h3><p><MapPin size={14} /> {activity.location || 'Địa điểm đang cập nhật'}</p></div><button type="button" aria-label={`Chi tiết ${activity.title}`} onClick={() => onOpen(activity)}><ArrowUpRight size={20}/></button></article>; }
